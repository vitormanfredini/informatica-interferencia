// use o "for" para fazer 2 filas de desenhos:
// uma fila de circles e uma fila de quadrados

function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(120,10,30);
  fill(30,170,40)
  
  circle(40,150,30)
  square(40,250,30)
  
}